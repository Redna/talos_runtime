let ws=null,state={},events=[],commit={},containers={},thinkActive=false;
function connect(){const proto=location.protocol==="https:"?"wss:":"ws:";ws=new WebSocket(proto+"//"+location.host+"/ws");ws.onopen=()=>{document.getElementById("ws-status").className="status-dot connected"};ws.onclose=()=>{document.getElementById("ws-status").className="status-dot error";setTimeout(connect,3000)};ws.onmessage=e=>{const msg=JSON.parse(e.data);handleMessage(msg)}}
function handleMessage(msg){switch(msg.type){case"full_snapshot":state=msg.state||{};events=msg.events||[];commit=msg.commit||{};containers=msg.container_status||{};renderAll();break;case"state_update":state={...state,...msg};renderState();renderHealth();break;case"stream_token":if(!thinkActive){startThink(msg.model)}appendToken(msg.content);break;case"tool_call":appendToolCall(msg.name,msg.arguments);break;case"tool_result":appendToolResult(msg.output,msg.success);break;case"think_start":startThink(msg.model);break;case"think_end":endThink(msg.tokens_in,msg.tokens_out,msg.context_pct);break;case"event":events.push(msg);renderEvents();break;case"container_status":containers=msg;renderContainers();break;case"commit_info":commit=msg;renderCommit();break}}
function startThink(model){thinkActive=true;const el=document.getElementById("stream-content");if(el.innerHTML){el.innerHTML+='<div class="think-separator">\u2014 think cycle \u2014</div>'}document.getElementById("stream-status").className="status-dot active";document.getElementById("stream-model").textContent=model||"\u2014";document.getElementById("stream-turn").textContent="Turn "+(state.turn||"\u2014")}
function endThink(tokensIn,tokensOut,contextPct){thinkActive=false;document.getElementById("stream-status").className="status-dot";if(tokensIn)document.getElementById("tokens-in").textContent="In: "+tokensIn;if(tokensOut)document.getElementById("tokens-out").textContent="Out: "+tokensOut;if(contextPct!==undefined)updateContextBar(contextPct)}
function appendToken(content){const el=document.getElementById("stream-content");let last=el.lastElementChild;if(!last||!last.classList.contains("token")){last=document.createElement("span");last.className="token";el.appendChild(last)}last.textContent+=content;el.scrollTop=el.scrollHeight}
function appendToolCall(name,args){const el=document.getElementById("stream-content");const div=document.createElement("div");div.className="tool-call";div.textContent="\u25b8 "+name+"("+Object.keys(args||{}).join(", ")+")";el.appendChild(div);el.scrollTop=el.scrollHeight}
function appendToolResult(output,success){const el=document.getElementById("stream-content");const div=document.createElement("div");div.className="tool-result";const short=(output||"").substring(0,200);div.textContent=success!==false?short:"\u2717 "+short;el.appendChild(div);el.scrollTop=el.scrollHeight}
function updateContextBar(pct){const fill=document.getElementById("context-fill");const text=document.getElementById("context-text");const pctNum=Math.round(pct*100);fill.style.width=pctNum+"%";text.textContent=pctNum+"%";if(pctNum<60)fill.style.backgroundColor="var(--green)";else if(pctNum<85)fill.style.backgroundColor="var(--yellow)";else fill.style.backgroundColor="var(--red)"}
function renderAll(){renderState();renderHealth();renderContainers();renderEvents();renderCommit()}
function renderState(){if(state.context_pct!==undefined)updateContextBar(state.context_pct);if(state.tokens_used!==undefined)document.getElementById("tokens-total").textContent="Total: "+state.tokens_used;if(state.turn!==undefined)document.getElementById("turn-count").textContent="Turn: "+state.turn;if(state.model)document.getElementById("model-info").textContent=state.model;if(state.spend!==undefined)document.getElementById("spend").textContent="Spend: $"+state.spend.toFixed(2)}
function renderHealth(){const el=document.getElementById("spine-status");el.textContent="Spine: "+(state.status||"unknown");if(state.status==="healthy")el.style.color="var(--green)";else if(state.status==="stalled")el.style.color="var(--red)";else el.style.color="var(--yellow)";document.getElementById("lazarus").textContent="Failures: "+(state.consecutive_failures!=null?state.consecutive_failures:"\u2014")}
function renderContainers(){const el=document.getElementById("container-dots");el.innerHTML="";for(const[name,status]of Object.entries(containers)){const d=document.createElement("div");d.className="container-dot";const dot=document.createElement("span");dot.className="dot";dot.style.backgroundColor=status==="healthy"?"var(--green)":status==="offline"?"var(--dim)":"var(--red)";d.appendChild(dot);d.appendChild(document.createTextNode(name));el.appendChild(d)}}
function renderEvents(){const el=document.getElementById("event-list");el.innerHTML="";const recent=events.slice(-50);for(const ev of recent){const div=document.createElement("div");let cls="event-item";const type=ev.type||ev.event_type||"";if(type.includes("restart"))cls+=" restart";else if(type.includes("crash"))cls+=" crash";else if(type.includes("override"))cls+=" override";else if(type.includes("started"))cls+=" started";div.className=cls;const ts=document.createElement("span");ts.className="ts";ts.textContent=(ev.ts||"").substring(11,19);div.appendChild(ts);const summary=type.replace("spine.","")+(ev.reason?" : "+ev.reason:"")+(ev.exit_code?" (exit "+ev.exit_code+")":"");div.appendChild(document.createTextNode(summary));el.appendChild(div)}el.scrollTop=el.scrollHeight}
function renderCommit(){const el=document.getElementById("commit-info");if(!commit.candidate){el.textContent="No commit info";return}let text="Candidate: "+commit.candidate.substring(0,8);if(commit.candidate_msg)text+=" \u2014 "+commit.candidate_msg;if(commit.stable)text+=" | Stable: "+commit.stable.substring(0,8);if(commit.ahead)text+=" | "+commit.ahead+" ahead";el.textContent=text}
async function sendCommand(cmd){await fetch("/api/command",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({command:cmd})})}
connect();